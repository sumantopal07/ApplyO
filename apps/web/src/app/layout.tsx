import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';
import { Toaster } from 'react-hot-toast';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'ApplyO - One profile. Infinite applications.',
  description: 'Eliminate repetitive job application form filling. Create your profile once and apply to any company instantly.',
  keywords: ['job application', 'career', 'recruitment', 'profile', 'auto-fill'],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
        <Toaster position="top-right" />
      </body>
    </html>
  );
}
